version: '3.6'
services:
  cen:
    image: ms-bff-json-statistics:1.0.0
    build: .
    ports:
      - "5000:5000"
    volumes:
     - ./app/app.py:/app/app.py
    environment:
      TIMEOUT: 10
      LOG_LEVEL: INFO
      TIMEZONE: America/Santiago
